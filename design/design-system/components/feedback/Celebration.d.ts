export interface CelebrationProps {
  /** Number of confetti papers; the app uses 70. */
  count?: number;
  /** Deterministic layout seed. */
  seed?: number;
}
export declare function Celebration(props: CelebrationProps): JSX.Element;
