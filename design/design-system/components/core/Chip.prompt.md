Multi-select chip for secondary settings — smaller than an OptionTile, still 44px tall.

```jsx
<Chip label="Sinh tồn" hint="5 mạng" selected />
```

Selected: accent border, `--accent-soft` fill, inset accent hairline plus a brand glow.
