Online chat, bottom row of the game screen. There is no free-text chat in this product — only this closed list of nine.

```jsx
<EmojiBar onSend={(e) => room.sendEmoji(e)} />
```

40px pills; the sent emoji also blasts up huge over the board and as a bubble on the sender's chip. Spectators don't get the bar.
